function convertNumber(value){
    var asciiNumber = value + 48;
      if (asciiNumber > 1000)
        console.log("Vive le Javascript");
      else
        return (asciiNumber);
  }
  convertNumber(99);
  